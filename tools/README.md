# tools

Developer utilities and scripts — e.g., the local multi-peer test harness used to validate
gossip convergence, anti-entropy repair, snapshot bootstrap, and partition/rejoin (M4+).
Not part of the shipped artifacts.
