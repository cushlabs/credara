# Ansible collections for deploy.yml. Install with:
#   ansible-galaxy collection install -r requirements.yml
# Also needs the `kubernetes` Python package in the Ansible environment:
#   pip install kubernetes
collections:
  - name: kubernetes.core
    version: ">=3.0.0"
